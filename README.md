# CatanPP

## Instructions

When opening the project in Unity, the editor will automatically detect the required Unity version based on the project settings. If you do not have the correct version installed, Unity Hub will prompt you to download and install the matching version.
