namespace Catan.GameMode
{
    /// <summary>High-level phases of a Catan match.</summary>
    public enum GamePhase
    {
        Setup,   // two free settlements & roads
        Play     // normal dice / build / trade loop
    }
}
